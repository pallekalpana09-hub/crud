# crud
This project is a Flask-based web application that performs basic CRUD (Create, Read, Update, Delete) operations. It provides a simple and interactive interface to manage data through a web browser. The backend is built using Python Flask, and the frontend uses HTML, CSS, and JavaScript.
